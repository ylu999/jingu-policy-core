import { Input, Violation } from "../../types";
/**
 * P10 — Core Tension First
 * Rejects reasoning frames that fail to articulate the core tension.
 */
export declare const p10CoreTension: (input: Input) => Violation[];
