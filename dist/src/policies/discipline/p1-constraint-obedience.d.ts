import { Input, Violation } from "../../types";
/**
 * P1 — Constraint Obedience
 * Detects actions that violate explicit task constraints.
 */
export declare const p1ConstraintObedience: (input: Input) => Violation[];
